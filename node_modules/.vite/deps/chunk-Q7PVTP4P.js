var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __publicField = (obj, key, value) => {
  __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
  return value;
};

// packages/shared/ReactSymbols.ts
var supportSymbol = typeof Symbol === "function" && Symbol.for;
var REACT_ELEMENT_TYPE = supportSymbol ? Symbol.for("react.element") : 60103;

// packages/react/src/jsx.ts
var ReactElement = function(type, key, ref, props) {
  const element = {
    $$typeof: REACT_ELEMENT_TYPE,
    type,
    key,
    ref,
    props,
    __mark: "KaSong"
  };
  return element;
};
var jsxDEV = (type, config) => {
  let key = null;
  const props = {};
  let ref = null;
  for (const prop in config) {
    const val = config[prop];
    if (prop === "key") {
      if (val !== void 0) {
        key = "" + val;
      }
      continue;
    }
    if (prop === "ref") {
      if (val !== void 0) {
        ref = val;
      }
      continue;
    }
    if ({}.hasOwnProperty.call(config, prop)) {
      props[prop] = val;
    }
  }
  return ReactElement(type, key, ref, props);
};

// packages/react/src/currentDispatcher.ts
var currentDispatcher = {
  current: null
};
var resolveDispatcher = () => {
  const dispatcher = currentDispatcher.current;
  if (dispatcher === null) {
    throw new Error("Invalid hook call. Hook只能在函数组件中执行resolveDispatcher()报错");
  }
  return dispatcher;
};
var currentDispatcher_default = currentDispatcher;

// packages/react/index.ts
var useState = (initialState) => {
  const dispatcher = resolveDispatcher();
  return dispatcher.useState(initialState);
};
var __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = {
  currentDispatcher: currentDispatcher_default
};
var react_default = {
  version: "0.0.0",
  createElement: jsxDEV
};

export {
  __export,
  __publicField,
  REACT_ELEMENT_TYPE,
  useState,
  __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED,
  react_default
};
//# sourceMappingURL=chunk-Q7PVTP4P.js.map
